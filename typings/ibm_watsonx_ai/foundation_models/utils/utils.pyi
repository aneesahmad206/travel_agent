"""
This type stub file was generated by pyright.
"""

from dataclasses import KW_ONLY, dataclass
from pathlib import Path
from string import Formatter
from typing import Any, Generator, Mapping, Sequence, TYPE_CHECKING
from ibm_watsonx_ai import APIClient

if TYPE_CHECKING:
    ...
@dataclass
class PromptTuningParams:
    base_model: dict
    _: KW_ONLY
    accumulate_steps: int | None = ...
    batch_size: int | None = ...
    init_method: str | None = ...
    init_text: str | None = ...
    learning_rate: float | None = ...
    max_input_tokens: int | None = ...
    max_output_tokens: int | None = ...
    num_epochs: int | None = ...
    task_id: str | None = ...
    tuning_type: str | None = ...
    verbalizer: str | None = ...
    def to_dict(self) -> dict:
        ...
    


@dataclass
class FineTuningParams:
    base_model: dict
    task_id: str
    _: KW_ONLY
    num_epochs: int | None = ...
    learning_rate: float | None = ...
    batch_size: int | None = ...
    max_seq_length: int | None = ...
    accumulate_steps: int | None = ...
    verbalizer: str | None = ...
    response_template: str | None = ...
    gpu: dict | None = ...
    peft_parameters: dict | None = ...
    gradient_checkpointing: bool | None = ...
    def to_dict(self) -> dict:
        ...
    


def get_model_specs(url: str, model_id: str | None = ...) -> dict:
    """
    Retrieve the list of deployed foundation models specifications.

    .. deprecated:: v1.0
       The ``get_model_specs()`` function is deprecated, use the :meth:`client.foundation_models.get_model_specs() <ibm_watsonx_ai.foundation_models_manager.FoundationModelsManager.get_model_specs>` function instead.

    :param url: URL of the environment
    :type url: str

    :param model_id: ID of the model, defaults to None (all models specs are returned).
    :type model_id: Optional[str, ModelTypes], optional

    :return: list of deployed foundation model specs
    :rtype: dict

    **Example:**

    .. code-block:: python

        from ibm_watsonx_ai.foundation_models import get_model_specs

        # GET ALL MODEL SPECS
        get_model_specs(url="https://us-south.ml.cloud.ibm.com")

        # GET MODEL SPECS BY MODEL_ID
        get_model_specs(
            url="https://us-south.ml.cloud.ibm.com", model_id="google/flan-ul2"
        )
    """
    ...

def get_custom_model_specs(credentials: dict | None = ..., api_client: APIClient | None = ..., model_id: str | None = ..., limit: int = ..., asynchronous: bool = ..., get_all: bool = ..., verify: str | Path | bool | None = ...) -> dict | Generator:
    """Get details on available custom model(s) as dict or as generator (``asynchronous``).
    If ``asynchronous`` or ``get_all`` is set, then ``model_id`` is ignored.

    .. deprecated:: v1.0
       The ``get_custom_model_specs()`` function is deprecated, use the :meth:`client.foundation_models.get_custom_model_specs() <ibm_watsonx_ai.foundation_models_manager.FoundationModelsManager.get_custom_model_specs>` function instead.

    :param credentials: credentials to watsonx.ai instance
    :type credentials: dict, optional

    :param api_client: API client to connect to service
    :type api_client: APIClient, optional

    :param model_id: Id of the model, defaults to None (all models specs are returned).
    :type model_id: str, optional

    :param limit:  limit number of fetched records. Possible values: 1 ≤ value ≤ 200, default value: 100
    :type limit: int, optional

    :param asynchronous:  if True, it will work as a generator
    :type asynchronous: bool, optional

    :param get_all:  if True, it will get all entries in 'limited' chunks
    :type get_all: bool, optional

    :param verify: You can pass one of following as verify:

        * the path to a CA_BUNDLE file
        * the path of directory with certificates of trusted CAs
        * `True` - default path to truststore will be taken
        * `False` - no verification will be made
    :type verify: bool | str | Path, optional

    :return: details of supported custom models
    :rtype: dict

    **Example:**

    .. code-block:: python

        from ibm_watsonx_ai.foundation_models import get_custom_model_specs

        get_custom_models_spec(api_client=client)
        get_custom_models_spec(credentials=credentials)
        get_custom_models_spec(
            api_client=client, model_id="mistralai/Mistral-7B-Instruct-v0.2"
        )
        get_custom_models_spec(api_client=client, limit=20)
        get_custom_models_spec(api_client=client, limit=20, get_all=True)
        for spec in get_custom_model_specs(
            api_client=client, limit=20, asynchronous=True, get_all=True
        ):
            print(spec, end="")

    """
    ...

def get_model_lifecycle(url: str, model_id: str) -> list | None:
    """
    Retrieve the list of model lifecycle data.

    .. deprecated:: v1.0
       The ``get_model_lifecycle()`` function is deprecated, use the :meth:`client.foundation_models.get_model_lifecycle() <ibm_watsonx_ai.foundation_models_manager.FoundationModelsManager.get_model_lifecycle>` function instead.

    :param url: URL of environment
    :type url: str

    :param model_id: the type of model to use
    :type model_id: str

    :return: list of deployed foundation model lifecycle data
    :rtype: list

    **Example:**

    .. code-block:: python

        from ibm_watsonx_ai.foundation_models import get_model_lifecycle

        get_model_lifecycle(
            url="https://us-south.ml.cloud.ibm.com",
            model_id="ibm/granite-13b-instruct-v2",
        )
    """
    ...

def get_model_specs_with_prompt_tuning_support(url: str) -> dict:
    """
    Query the details of the deployed foundation models with prompt tuning support.

    .. deprecated:: v1.0
       The ``get_model_specs_with_prompt_tuning_support()`` function is deprecated, use the :meth:`client.foundation_models.get_model_specs_with_prompt_tuning_support() <ibm_watsonx_ai.foundation_models_manager.FoundationModelsManager.get_model_specs_with_prompt_tuning_support>` function instead.

    :param url: URL of environment
    :type url: str

    :return: list of deployed foundation model specs with prompt tuning support
    :rtype: dict

    **Example:**

    .. code-block:: python

        from ibm_watsonx_ai.foundation_models import (
            get_model_specs_with_prompt_tuning_support,
        )

        get_model_specs_with_prompt_tuning_support(
            url="https://us-south.ml.cloud.ibm.com"
        )
    """
    ...

def get_supported_tasks(url: str) -> dict:
    """
    Retrieves a list of tasks that are supported by the foundation models.

    :param url: URL of the environment
    :type url: str

    :return: list of tasks that are supported by the foundation models
    :rtype: dict

    **Example:**

    .. code-block:: python

        from ibm_watsonx_ai.foundation_models import get_supported_tasks

        get_supported_tasks(url="https://us-south.ml.cloud.ibm.com")
    """
    ...

def get_all_supported_tasks_dict(url: str = ...) -> dict:
    ...

def load_request_json(run_id: str, api_client: APIClient, run_params: dict[str, Any] | None = ..., **kwargs: Any) -> dict[str, Any]:
    ...

async def aload_request_json(run_id: str, api_client: APIClient, run_params: dict[str, Any] | None = ..., **kwargs: Any) -> dict[str, Any]:
    ...

def is_training_prompt_tuning(training_id: str | None = ..., api_client: APIClient | None = ..., **kwargs: Any) -> bool:
    """Returns True if training_id is connected to prompt tuning"""
    ...

async def ais_training_prompt_tuning(training_id: str, api_client: APIClient) -> bool:
    """Returns True if training_id is connected to prompt tuning asynchronously"""
    ...

class TemplateFormatter(Formatter):
    def check_unused_args(self, used_args: set[(int | str)], args: Sequence, kwargs: Mapping[str, Any]) -> None:
        """Check for unused args."""
        ...
    


class HAPDetectionWarning(UserWarning):
    ...


class PIIDetectionWarning(UserWarning):
    ...


class GraniteGuardianDetectionWarning(UserWarning):
    ...


class LifecycleWarning(UserWarning):
    ...


class WatsonxLLMDeprecationWarning(UserWarning):
    ...


def get_embedding_model_specs(url: str) -> dict:
    """
    .. deprecated:: v1.0
       The ``get_embedding_model_specs()`` function is deprecated, use the :meth:`client.foundation_models.get_embeddings_model_specs() <ibm_watsonx_ai.foundation_models_manager.FoundationModelsManager.get_embeddings_model_specs>` function instead.
    """
    ...

